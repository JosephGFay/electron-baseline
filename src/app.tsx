import * as ReactDOM from 'react-dom';

import VMCard from './components/VMCard';
function render() {
    ReactDOM.render(<>
        <div className='bg-zinc-900'>
        </div>
    </>, document.body);
}

render();